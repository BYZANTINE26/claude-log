# Backlog

Tickets parked for later. Each entry: flag, one-line summary, and where/when
it was parked.

## technical-debt
- Mid-session crash (Stop hook never fires) leaves stale turn-buffer data
  that silently merges into the next turn's summary. Parked during Core
  Logging MVP planning (2026-09-11) — accepted as an MVP limitation, see
  `.claude/plans/PLAN.md`.
- No file locking on the session log; two sessions sharing a `session_id`
  would corrupt it (single-writer assumption, unhandled). Parked during Core
  Logging MVP planning (2026-09-11).
- No log rotation or retention policy — session logs grow unbounded. Parked
  during Core Logging MVP planning (2026-09-11).
- After /clear the summarization should not use past N logs for summarization
  as it's a new context window, unless user continues the context by ingesting
  las K logs into context. If K < N, use only K records for summarization and
  grow it to N by doing +1 at each turn as new records are accumulated.
- simple summaries in case of failures is not a good option, the user
  must be notified and the logger should not put in any temporary logs,
  for any kind of failure scenario.
- hook monitoring of <1 seconds and 5 second timeout needs to be
  reconsidered.
- hook is buffering or not?
- ship this as plugin which will work with any claude code session on 
  that machine right of the back.
- stop logging with /stop or /exit cmds but then what about background
  sessions logging, also how will CTRL+C inside claude session stop logging.
- a traditional logger for this program itslef
- backlog template in claude.md + strictly follow atomic commits + our workflow
  methodology.
- don't add a co-authered by claude flag in commits.
- add comments for better understanding of code.
- are we not using the turn id generated by claude for its session
  logging?
- why to read file again and again for recent entries or for entry
  count, once at the begining is fine but from there it should be in
  memory itself or in a tmp file updated with each turn.
- the buffer for user_prompt + tool_input + tool_output + claude
  response can also be in memory I believe. And we are using claude's
  response in summarization right....?
- this whole tool needs to be shipped as a proper cluade code plugin

## feature
- Real local summarization endpoint integration (e.g. Ollama) to replace the
  rule-based stub summarizer. Parked during Core Logging MVP planning
  (2026-09-11) — stub is deliberate for MVP, see `.claude/plans/PLAN.md`.
- Benchmarking suite comparing claude-log vs. claude-mem on token usage,
  context fidelity, and resumability. This is an INTENT.md success
  criterion, tracked as its own phase after the MVP — not blocking it.
  Parked during Core Logging MVP planning (2026-09-11).
- Manual cmd for user to ingest the log entries (configurable last K) into
  context.

## good-to-have
- Viewing/querying UI or CLI for browsing a session log. Parked during Core
  Logging MVP planning (2026-09-11).
- Export a session log to markdown/CSV; search/filter across multiple
  session logs. Parked during Core Logging MVP planning (2026-09-11).
- Compression/encryption for archived log files. Parked during Core Logging
  MVP planning (2026-09-11).




okay so I went through the code on this branch and there are a lot of things not taken into consideration while planning, implemented wrong, not to my liking, we need to scrap this code branch, and even redo the planning and stuff. first take a quick look to the unstaged changes except to claude file which was just renamed as I put all those instructions at user level claude file, look at other changes and you will get a little idea, but keep in mind those are not all the changes we need to do, that's just for a idea. Go through them get an Idea and then I will tell the next steps.